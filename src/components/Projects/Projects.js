import React from 'react';
import './Projects.css';

class Projects extends React.Component {
    render() {
        return (
            <div>
                <p>Modals for each project are going here</p>
                <p>I'd also like a filter feature, where users can filter modals by language</p>
            </div>
        );
    }
}

export default Projects;